---
layout: default
layout_type: internal_page
title: Últimas notícias
category: noticias
---
%ul#news
  - site.posts.each do |post|
    - if post.categories.include?("noticias") && post.title != 'Últimas notícias'
      %li
        %a{ :href => post.url}= post.title
